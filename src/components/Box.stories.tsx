import { Meta, StoryObj } from "@storybook/react";
import { Box } from "./Box";

export default {
  title: "Components/Box",
  component: Box,
  args: {},
} as Meta

export const Default: StoryObj = {}; 